# Use legacy peer dependency resolution so npm install succeeds.
# The app uses React 19; react-day-picker@8 declares peer react@^16-18.
# See README for yarn as an alternative.
legacy-peer-deps=true
