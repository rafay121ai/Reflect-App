# LLM provider: ollama (dev) | openrouter | openai | anthropic | …
LLM_PROVIDER=ollama

# Ollama (when LLM_PROVIDER=ollama)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen

# OpenRouter (when LLM_PROVIDER=openrouter) – single API for many models
# Get key: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=deepseek/deepseek-r1-0528

# Supabase – required for storing reflections, mood, reminders in DB
# 1. pip install -r requirements.txt  (installs supabase)
# 2. In Dashboard → Settings → API (or API Keys): use the new secret key (sb_secret_...) for SUPABASE_SERVICE_KEY
# 3. In Supabase SQL Editor, run: supabase_schema.sql, supabase_migration_reflections.sql, etc.
SUPABASE_URL=https://your-project.supabase.co
# Use the new Secret API key (sb_secret_...), not the legacy service_role JWT. See SUPABASE_KEYS.md.
SUPABASE_SERVICE_KEY=your-secret-key-sb_secret_xxx
# Optional. Backend verifies user JWTs via ES256 (JWKS). Only needed for legacy HS256 tokens.
# SUPABASE_JWT_SECRET=your-jwt-secret

# Optional: secret for POST /api/personalization/refresh-all (manual/cron). Set and pass as X-Cron-Secret header or ?secret=.
# PERSONALIZATION_CRON_SECRET=your-cron-secret

# Auto-refresh personalization context for all users (default: 24 = every 24 hours). Set to 0 to disable.
# PERSONALIZATION_REFRESH_INTERVAL_HOURS=24

# CORS – required in production. Comma-separated list of frontend origins (e.g. your Vercel URL).
# ALLOWED_ORIGINS=https://your-app.vercel.app
