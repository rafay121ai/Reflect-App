# Single worker: acceptable for beta. For production scale, switch to:
# gunicorn server:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
web: uvicorn server:app --host 0.0.0.0 --port $PORT
